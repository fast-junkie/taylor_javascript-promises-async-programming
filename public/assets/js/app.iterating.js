import {
  get, getCatch, chain, concurrent, parallel,
} from '../src/module/iterating.mjs';

window.get = get;
window.getCatch = getCatch;
window.chain = chain;
window.concurrent = concurrent;
window.parallel = parallel;
