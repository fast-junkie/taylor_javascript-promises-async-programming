import {
  get, getCatch, chain, chainCatch, final,
} from '../src/module/consuming.mjs';

window.get = get;
window.getCatch = getCatch;
window.chain = chain;
window.chainCatch = chainCatch;
window.final = final;
